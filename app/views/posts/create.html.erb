<div class="container text-center my-5 align-middle">
    <% if @post.errors.any? %>
        <div id="error_explanation">
            <h2>Se ha encontrado <%= pluralize(@post.errors.count, "error(es)") %> y no se ha podido enviar el post por la(s) siguiente(s) razon(es):</h2>
            <ul>
                <% @post.errors.full_messages.each do |msg| %>
                    <li class="text-left"><%= msg %></li>
                <% end %>
            </ul>
        </div>
    <% else %>
        <h1> Post creado con exito</h1>
    <% end %>
    <div class= "text-center">
        <button type="button" class="btn btn-success btn-lg my-5 mx-auto"><%= link_to "Ver Articulos", pages_index_path %></button>
  </div>
</div>


